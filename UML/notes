Classes
   Location
      int x, y, Z
      String name
   
   Map
      Hashmap<Landmarks> marks
      Location x
      Edge e

   Edge
      Location start, end
      int distance

   Landmarks extends Location (specific location)
      Location landmark
      boolean canWalk

   Business extends Locations (specific location)
      Time operations





Solar Crank Game
   crank -> gears -> e;ectric generator
   interface on android table (possibly Arduino led's)
   VR or different solar panels to play with
   How crank works:  TODO
   difficulty affects energy generated by panel (not user)


IMPORTANT: get UML/ get map working
